import {nanoid} from 'nanoid';import type {Config} from './config.js';import {logDiagnostic,redactedHeaders} from './diagnostics.js';import {sanitise,tokenStore} from './security.js';
export class ApiProblem extends Error{constructor(readonly code:string,readonly correlationId:string){super(code)}}
export async function apiRequest<T>(config:Config,path:string,init:RequestInit={},onLeak=()=>{},approved?:ReadonlySet<string>):Promise<T>{const correlationId=nanoid();const headers=new Headers(init.headers);headers.set('Authorization',`Bearer ${tokenStore.get()??''}`);headers.set('X-Correlation-ID',correlationId);headers.set('Content-Type','application/json');if((init.method??'GET')!=='GET')headers.set('Idempotency-Key',crypto.randomUUID());const started=performance.now();const response=await fetch(`${config.runtimeApiBase}/${path}`,{...init,headers});const body=await response.json() as Record<string,unknown>;logDiagnostic({kind:'http',correlationId,status:response.status,durationMs:performance.now()-started,errorCode:typeof body.code==='string'?body.code:undefined,headers:redactedHeaders(headers)});if(!response.ok)throw new ApiProblem(typeof body.code==='string'?body.code:'UNKNOWN_ERROR',typeof body.correlation_id==='string'?body.correlation_id:correlationId);return sanitise(body,onLeak,approved) as T}
